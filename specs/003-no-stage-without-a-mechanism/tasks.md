# Tasks: Nenhuma etapa sem mecanismo

- T201: Reconciliar `.github/rulesets/main.yml` com a configuracao aplicada e implementar controle que compare ficheiro e resposta da API. Mecanismo: controle em integracao continua. Refs: FR-001, FR-002, AC-006.
- T202: Executar tentativa de push directo para a branch por omissao e registar a rejeicao observada. Mecanismo: ruleset no servidor. Refs: AC-001.
- T203: Configurar `core.hooksPath` no bootstrap e implementar rejeicao em `doctor` e `verify` quando os hooks de cliente nao estiverem activos. Mecanismo: comando do pipeline e controle em integracao continua. Refs: FR-003, AC-002.
- T204: Arbitrar a coexistencia entre `.pre-commit-config.yaml` e `scripts/git-hooks`, registar a decisao e implementar controle que rejeite ambos activos simultaneamente. Mecanismo: decisao registada e controle executavel. Refs: FR-004.
- T205: Implementar controle de harness que rejeite invocacao de escrita em git fora do pipeline. Mecanismo: `PreToolUse`, avaliado pelo harness anteriormente a execucao da ferramenta. Refs: FR-011, AC-007.
- T206: Documentar o alcance de cada mecanismo, incluindo a limitacao do controle de harness a estacao de trabalho configurada. Mecanismo: texto adjacente ao controle e teste que o verifica. Refs: FR-012, FR-009.
- T207: Implementar recibo por etapa e rejeicao de etapa cujo antecessor nao possua recibo valido. Mecanismo: artefacto de recibo lido pela etapa subsequente. Refs: FR-005, AC-003.
- T208: Emitir attestation em `quality.yml`, assinada pela identidade OIDC da execucao, com o digest do conteudo como sujeito. Mecanismo: identidade da execucao, inacessivel ao executor da entrega. Refs: FR-006, AC-004.
- T209: Reclassificar `last_verified_commit` como cache de leitura do veredicto assinado. Mecanismo: verificacao da attestation em substituicao a leitura do ficheiro de estado. Refs: FR-007.
- T210: Implementar rejeicao de caso de teste ignorado sem justificativa declarada. Mecanismo: asserçao sobre o relatorio de execucao, nao sobre o codigo de saida. Refs: FR-008, AC-005.
  - Entregue em `tests/conftest.py` mais `tests/skip_policy.py`. Distincao aplicada: razao escrita nao e razao declarada -- `pytest.skip("x")` carrega razao e nao declara nada. O inventario versionado recusa skip nao declarado, declaracao reaproveitada para outra razao, e declaracao que deixou de acontecer. Alcance declarado: vigora na suite deste repositorio; projecto derivado mantem a sua propria suite e nao herda o inventario.
- T211: Revisar a especificacao contra a FR-010, confirmando mecanismo nomeado por requisito. Mecanismo: revisao manual. Unica tarefa desta lista sem mecanismo automatizado, condicao registada explicitamente. Refs: FR-010, AC-008.
- T212: Implementar verificacao da cadeia de rastreabilidade entre pull request e requisito do PRD, com identificacao nominal do elo ausente. Mecanismo: controle em integracao continua sobre o evento de pull request. Refs: FR-013, AC-009.
  - Entregue em `delivery.traceability_refusal`. A mae descobre-se por `parent_issue_url`, que vem no payload da propria issue -- medido em 2026-09-21 contra a #42, que devolveu a URL da #26. Aceita duas formas, ambas medidas no quadro deste repositorio: sub-issue cuja mae nomeia `specs/NNN/` existente, ou issue solta que a nomeia. Excepcao declarada por `no_spec_reason`, porque as issues #15, #36, #45 e #46 sao correccoes sem especificacao propria e uma regra que as recusasse seria removida.
  - Etapa 6 fechada. Etapa 20 fica `parcial`: o elo ate ao requisito do PRD nao existe para ser verificado, por dois factos medidos -- o PRD e um template com 80 `TODO` (#46) e `FR-001` designa quatro requisitos diferentes nas quatro specs (#45).
  - Nomear nao e ter: a issue #28 nomeia `specs/005-agent-throughput`, que nao existe, e o portao recusa-a. Uma ligacao para documento que ninguem escreveu e a forma da rastreabilidade sem a substancia.
- T213: Implementar rejeicao de alteracao de codigo nao associada a especificacao activa. Mecanismo: controle em integracao continua sobre os caminhos alterados pela pull request. Refs: FR-014, AC-010.
  - Associacao medida no diff, nao no campo de estado. Medido em 2026-09-21, nos 8 commits de primeiro progenitor ate `48639d30`: todos declaravam `specs/001` enquanto fechavam as specs 002 a 004, portanto uma regra sobre a existencia do campo teria aprovado 8 em 8 sem medir nada. Escape declarado, por decisao do proprietario: `no_spec_reason` no estado, recusado quando curto demais para alguem agir sobre ele. Caminhos vindos de `gh pr view --json files`, porque `fetch-depth: 2` nao alcanca a base.
- T214: Implementar verificacao automatizada da matriz de cobertura, rejeitando classificacao `ausente` remanescente. Mecanismo: teste que le a propria matriz, de modo a impedir desactualizacao silenciosa. Refs: FR-016, AC-011.
  - Desvio registado face ao enunciado: a rejeicao literal de `ausente` nasceria vermelha, com quatro etapas ainda abertas, e um teste que nasce vermelho e desligado. O mecanismo entregue e inventario fixado por classificacao aberta, mais citacao verificavel por linha, mais a AC-011 como skip que reporta quantas etapas faltam em cada execucao. A rejeicao literal entra sozinha quando o inventario esvaziar. Ver `tests/unit/test_coverage_matrix_is_measured.py`.
- T222: Verificar que o artefacto citado por cada linha da matriz implementa de facto a etapa que a linha nomeia. Medido em 2026-09-21: a T214 verifica coordenacao e existencia, nao adequacao -- uma linha pode citar um teste real que testa outra coisa. Mecanismo por definir. Refs: FR-016, NFR-005.
- T215: Implementar suite adversarial do pipeline, com uma tentativa por vetor de bypass enumerado e asserçao sobre a rejeicao observada. Mecanismo: suite executada em integracao continua. Refs: FR-018, AC-013.
  - Entregue como arreio de mutacao em `src/engineering_playbook/mutation.py`, com job `adversarial` proprio em integracao continua. O que a suite afirma e estreito e declarado: cada mecanismo do inventario e sustentado por um teste que falha sem ele. Nao afirma ausencia de vectores, conforme a NFR-005.
- T216: Executar teste de mutacao de controle: remover cada mecanismo individualmente e confirmar a falha da suite adversarial. Mecanismo: a propria suite, avaliada contra a ausencia do controle. Refs: FR-017, AC-012.
  - Medido em 2026-09-21: 14 mecanismos, 14 apanhados, com alcance de 6 das 20 etapas da matriz. Corrigido em #61, que mediu o inventario em vez de repetir o numero: sao 22 mecanismos, 22 apanhados, com alcance de 7 etapas -- 4, 9, 15, 16, 17, 18, 20. A celula da matriz dizia `6 das 20 -- 4, 6, 9, 15, 16, 17`, e nomeava a etapa 6, que o inventario nunca alcancou, enquanto omitia a 18 e a 20, que alcanca. `test_coverage_matrix_is_measured.py` passou a comparar a celula com `COVERED_STAGES`, para que a terceira ocorrencia deste defeito seja vermelha e nao apenas lamentada. Tres defeitos do proprio arreio apareceram e estao fechados: uma entrada que mutava a redaccao de uma recusa em vez da recusa; outra cujo texto ja mudara de sitio; e -- o mais grave, encontrado em revisao -- `caught` a significar apenas `returncode != 0`, o que fazia uma entrada apontada a um ficheiro ja vermelho no sandbox reportar deteccao de graca. A corrida limpa passou a ser obrigatoria e a captura exige um node id que passou a falhar.
- T217: Registar na suite adversarial o limite de cobertura declarado na NFR-005, com teste que verifique a presenca do registo. Mecanismo: teste sobre o texto da suite. Refs: NFR-005.
  - Entregue: o limite da NFR-005 esta registado no docstring de `tests/unit/test_the_pipeline_attacks_itself.py` e verificado por `test_the_suite_records_what_it_does_not_claim`, que le o texto da propria suite conforme o mecanismo que a tarefa enuncia.
- T218: Implementar medicao, em `start`, de que o HEAD de partida esta contido na base, com `--from-base` como saida dentro da esteira. Mecanismo: verificacao de alcancabilidade no proprio comando, com teste adversarial que planta o estado pos-squash e assere a recusa. Refs: FR-005, FR-009.
- T219: Unificar o referente de `--base` entre `start`, `publish` e `status`. Medido em 2026-09-21: `start` resolve `refs/remotes/<remote>/<base>` e `publish`/`status` resolvem `refs/heads/<base>`, de modo que `publish` conta como locais commits que a base remota ja tem. Mecanismo: um unico resolvedor de base, com teste que falha perante dois referentes. Refs: FR-005.
  - Entregue como `resolve_base`, usado por `publish` e `status`; `start` mantem a sua recusa propria, porque partir de base nao medida e o defeito da #36. Medido nesta arvore em 2026-09-21: 8 commits contra `refs/heads/main` e 0 contra `refs/remotes/origin/main`. Fallback declarado: sem ref remoto usa-se o ramo local, porque um repositorio que nunca buscou nao tem melhor resposta. `status` ganhou `--remote`, que aceitava `--base` e nao aceitava o remoto a que a base pertence.
  - Corrigido em #61. O resolvedor tinha teste; usa-lo nao tinha. Medido em copia sandbox: revertendo os dois sitios de chamada para o referente anterior, a suite inteira ficou com exactamente as falhas da linha de base -- os 480 testes que a suite tinha em 2026-09-21, antes desta fatia lhe acrescentar os que faltavam, nenhum novo a vermelho. A entrada do inventario de mutacao mutava uma linha *dentro* de `resolve_base`, portanto media o mesmo que os testes ja mediam. Agora ha teste ponta a ponta por comando no cenario pos-squash (`command_status` reporta 0 e nao 1; `command_publish` recusa por `no local commit` onde antes passava o seu proprio portao) e duas entradas de mutacao adicionais, uma por sitio de chamada.
  - Corrigido tambem o fallback: devolvia o nome DWIM, que um tag homonimo vence, o que reabria o defeito que `base_ref_name` existe para fechar. Devolve `refs/heads/<base>` por extenso, e `status` imprime o ref que usou, para que a declaracao nao viva so no docstring.
- T220: Registar em artefacto legivel por instrumento que uma branch nasceu sob `--allow-unmerged-head`. Medido: o override so existe como linha impressa, portanto uma auditoria posterior nao o consegue observar. Mecanismo: campo em `.project/delivery/` lido pelas etapas seguintes. Refs: FR-009, FR-012.
  - Entregue como `.project/delivery/branch-origin.yml`, escrito por `start` e lido por `status`. Guarda `started_from`, o commit resolvido antes do switch: a primeira versao guardava o literal `HEAD`, que no momento da leitura ja designa a branch nova, e portanto dizia que houve excepcao sem dizer de onde a branch nasceu. Registado nos dois sentidos: um registo que so existe quando a excepcao foi usada le-se, para quem percorre a saida, exactamente como uma linha que alguem se esqueceu de escrever.
  - Alcance corrigido face ao enunciado. A tarefa dizia `campo lido pelas etapas seguintes`; o que foi entregue e lido por `status`, que e um comando de diagnostico. `prepare`, `commit`, `publish` e `merge` nao o consultam, e `.project/delivery/` e scratch ignorado pelo git, portanto o registo serve o operador e a fatia corrente e nao a auditoria posterior. Transportar os campos para o checkpoint versionado fecharia essa diferenca e e trabalho por fazer, registado na T223.
- T226: O repositorio fala uma lingua, e o contrato da issue passa a ter mecanismo. Medido em 2026-09-22 (#65), depois da ultima edicao desta fatia: 114 ficheiros rastreados com portugues, de um inventario cujas tres contagens vivem no cabecalho de `tests/unit/test_the_repository_speaks_one_language.py` e sao reconferidas por `test_the_numbers_in_this_file_are_the_numbers_it_measures` a cada corrida, e 39 issues corrigidas a mao -- 19 corpos, 2 titulos, 2 sem etiqueta, 2 a afirmar `Sub-issue of #26` sem que a API reportasse esse pai. Todas tinham atravessado todos os portoes existentes. Refs: FR-013, FR-015, FR-016, NFR-002, etapas 6 e 11.
  - O inventario move-se tambem por efeito colateral de trabalho fora desta spec: preencher o PRD (#46) traduziu conteudo real para ingles e o cabecalho caiu, sem qualquer edicao ao detetor ou ao guarda -- o numero exato fica de fora desta frase de proposito, porque esta propria tarefa e portuguesa e esta no inventario, e escrever o numero aqui muda-o outra vez. Registado porque o portao "especificacao reivindica o codigo" (FR-014) exige que a spec ativa cubra qualquer ficheiro de codigo tocado, incluindo este.
  - Entregue como dois mecanismos. `tests/unit/test_the_repository_speaks_one_language.py` le os ficheiros rastreados e fixa o inventario dos que ainda falam portugues, nos DOIS sentidos: um ficheiro que comeca a falar falha, e um nome que ficou depois da traducao tambem falha. O inventario a encolher e o relatorio de progresso da #65. `validate-ci --issue-contract` le pela API a issue que a pull request fecha e recusa titulo ou corpo fora do ingles, ausencia de etiqueta, e afirmacao de paternidade que a API nao confirma.
  - Revisao independente devolveu catorze achados, dois deles fatais para a fatia. O passo do workflow nao tinha `GH_TOKEN`, portanto o mecanismo seria vermelho em toda e qualquer pull request; e lia o corpo do payload do evento, que e a corrida que o passo seguinte documenta no mesmo ficheiro como ja paga. Os dois fecharam com a mesma mudanca: o corpo e lido ao vivo e a issue e encontrada por `issue_from_pr_body`, a funcao que ja existia testada, em vez de uma segunda regex em shell que era mais estreita e morria sem mensagem sob `bash -e`.
  - A deteccao passou a viver em `src/engineering_playbook/language.py`, uma so vez. Havia duas copias e ja divergiam: 83 palavras numa e 58 noutra. E `com` casava com `.com`, portanto o titulo `Pin the docs link to docs.github.com` era recusado como nao-ingles; resolvido nas fronteiras do padrao, o que tambem apanhou `para-virtualized` e `sem-ver`. Citacoes em backticks deixaram de contar: o corpo da propria #65, escrito em ingles SOBRE portugues, era recusado.
  - O filtro de extensoes escondia 44 linhas em dois ficheiros sem extensao (`scripts/git-hooks/pre-push` e o espelho) enquanto o docstring afirmava nomear todos. E nada segurava o passo do workflow: apaga-lo das duas copias deixava a suite inteira verde. `verify_root` assere-o agora, com teste e entrada de mutacao, pelo mesmo padrao que a T213 aplicou ao `--pr-files`.
  - Limiar medido e nao arbitrado: uma marca num titulo, duas numa linha de corpo. Um titulo e curto e nunca chega a duas -- o titulo real `publish e merge escrevem bookkeeping depois do push` carrega exactamente uma. Medido sobre os 39 titulos, o limiar de uma produziu um unico falso positivo, `TODOs`, que colide com `todos` e e vocabulario ingles deste repositorio; removido antes de medir em vez de tirado da lista de palavras.
  - Dois defeitos proprios encontrados ao construir isto. O `apply_mutation` da #49 preservava terminadores e deixou de casar literais multi-linha numa arvore CRLF, que e o estado normal de um checkout Windows: passou a normalizar para casar e a escrever pelo terminador do ficheiro. E o teste que o segurava comparava a CONTAGEM de CRLF, que muda de forma legitima quando a substituicao tem outro numero de linhas; passou a comparar o ESTILO.
- T225: Derivar a lista de exclusoes da paridade do espelho de `.project/distribution.yml` em vez de a manter em prosa no teste. Medido em 2026-09-22 (#49): `_EXCLUDED_PAIRS` tem tres caminhos e os tres constam do mapa `generated:` do `distribution.yml`; estao correctos por disciplina, que e o estado que a T224 recusa chamar mecanismo. Adiado deliberadamente da #49 por mudar a fonte de verdade de um teste que essa fatia ja reescreveu por inteiro, e registado aqui porque um adiamento que vive so na conversa nao sobrevive a fatia -- o mesmo argumento que a T220 faz sobre `.project/delivery/` ser scratch ignorado pelo git. Refs: FR-018, etapa 11 da matriz.
- T224: Retirar da paridade do espelho as exclusoes cuja razao expirou. Medido em 2026-09-22 (#49): `tests/unit/test_resource_mirror_parity.py` excluia `.project/schemas/*` e `.github/workflows/quality.yml` porque outro workstream os editava; a razao expirou e o workflow foi editado a mao varias vezes numa sessao, identico dos dois lados por disciplina e nao por mecanismo (sem numero exacto: nenhum instrumento deste repositorio o reproduz). Refs: FR-018, etapa 11 da matriz.
  - Entregue. Os sete schemas ja eram byte a byte identicos, portanto a exclusao nao protegia nada: passaram a ser pinados e os pares medidos subiram de 78 para 85. O workflow nao pode ser pinado byte a byte porque a copia da raiz traz o job `adversarial`, que um projecto derivado nao consegue correr -- instala nem `scripts/mutation.py` nem o inventario que ele le, e como o `attest` depende dele o veredicto assinado deixaria de ser emitido la. A copia distribuida e comparada contra a copia da raiz com esse job removido e com o `needs:` reescrito, de duas maneiras que se cobrem uma a outra: estrutural, sobre o YAML carregado, imune a formatacao; e textual, entre ancoras literais asseridas como unicas, que apanha comentarios e espacos. A revisao independente mediu a primeira versao -- que removia o bloco por varrimento de indentacao -- a falhar aberta com uma linha em branco antes do `attest:`, e a dar vermelho num espelhamento correcto da mesma linha; e mediu o corpo do job a chegar a copia distribuida sem o cabecalho, com o teste verde e a sua propria mensagem de erro a descrever o estado que deixara passar. Fechado, e o alcance esta declarado: as linhas em branco coladas ao job seguinte nao sao comparadas, e os terminadores de linha passaram a ser asseridos a parte porque `read_text` normaliza `\r\n` e escondia a deriva.
  - Duas entradas novas no inventario de mutacao, etapa 11, que o inventario nunca alcancara. Seguem o precedente da guarda da matriz: a guarda E um teste, portanto o que se muta e o DADO que ela le -- um schema distribuido que deriva, e um passo acrescentado na raiz que ninguem espelhou. Ambas apanhadas.
- T223: Transportar `branch_from`, `branch_started_from` e `branch_override` para o checkpoint versionado, que e o artefacto que sobrevive a fatia. Medido em 2026-09-21: o registo de origem vive em `.project/delivery/`, ignorado pelo git, portanto uma branch nascida sob excepcao chega a `publish` e a `merge` sem que isso conste de artefacto versionado nenhum. Refs: FR-009, FR-012.
- T221: Alargar o controlo de harness a `git switch`, `git checkout -b` e `git branch`. Medido em 2026-09-21 contra `.claude/hooks/enforce_delivery_pipeline.py`: `WRITE_VERB` cobre `commit|push|merge|rebase|reset` e `branch` esta na lista de leitura, portanto a criacao de branch fora da esteira nao e apanhada. Depende da T218, sem a qual o operador ficaria sem caminho ate a base. Refs: FR-011, AC-007.
  - Entregue no hook do harness. A dificuldade era distinguir leitura de escrita: `git branch` sem argumento, ou so com `-a`, `-r`, `--list`, `--show-current`, e como qualquer pessoa olha para o repositorio, e um controlo que recusa leitura e desligado. Alcance declarado pela FR-012 -- o hook nao e distribuido aos projectos derivados, so `.claude/agents` o e, portanto vigora na estacao que o tenha configurado e em mais lado nenhum.
  - Corrigido em #61. A primeira medicao cobriu 16 formas e a classificacao dizia `fechado`. A revisao mediu 51 e encontrou seis familias que passavam, entre elas o proprio comando que a linha da matriz nomeia com uma opcao global a frente (`-c k=v`), a copia e a movimentacao forcadas de branch, `worktree add -b`, `checkout --orphan`, `update-ref` e refspec de `fetch`. Encontrou tambem recusa indevida: `[^|&;]` nao excluia a quebra de linha, portanto o `-c` da linha seguinte de um bloco multi-linha era lido como flag do `switch` anterior. Agora sao 151 formas em `tests/unit/test_one_base_one_meaning.py`, 81 recusas e 70 leituras, mais catorze em `tests/unit/test_delivery_pipeline_hook.py` para o mesmo prefixo aplicado aos verbos antigos, e o vector passou a `parcial` -- a enumeracao e uma lista e nao uma prova de exaustao, e a guarda so vigora onde o harness esta configurado.
  - Limite medido e declarado no cabecalho do hook: a decisao le a string inteira do comando, de proposito, para apanhar formas compostas, e por isso recusa tambem *escrever sobre* estes comandos. 221 linhas em 32 dos 374 ficheiros versionados seriam recusadas se chegassem como comando de shell, 121 delas pela guarda de branch. O `pre-commit` era uma delas: dava como conselho um comando que o hook recusa, e passou a apontar primeiro para `delivery.py start`.
- T227: Nenhuma etapa deixa arvore suja atras de si. Medido em 2026-09-22 (#64): a entregar a #61, com arvore limpa depois do `commit`, o `publish` devolvia `M .project/state.yml`, `M .project/last-ci-run.yml` e um checkpoint por rastrear; o `commit` limpava; o `publish` sujava outra vez; o `merge` sujava outra vez. Tres ciclos. Como o `start` recusa arvore suja, e correctamente, o pipeline bloqueava a propria fatia seguinte, e a saida era commitar bookkeeping a mao com mensagem inventada ou deitar fora o que o pipeline acabara de escrever -- as duas coisas aconteceram duas vezes. Refs: FR-001, AC-001, etapa 9. Mesma classe da #12, uma etapa adiante.
  - Terceira ocorrencia da linha 2 do ledger nesta mesma fatia, e por isso fica escrita: os numeros da T226 foram reescritos aqui com o valor medido ANTES da ultima edicao, enquanto o comentario do guarda recebia o valor depois dela. Mediu-se uma vez e escreveu-se em dois sitios com resultados diferentes. So o CONJUNTO de 114 ficheiros esta preso por instrumento (`STILL_PORTUGUESE`, nos dois sentidos); a contagem de linhas e a de ficheiros lidos estao presas pelo teste do cabecalho, e as copias em prosa nao estao presas por nada. A contagem de linhas saiu daqui por uma segunda razao, medida ao tentar corrigi-la: esta tarefa e portuguesa e esta no inventario, portanto escrever o numero muda o numero. Duas tentativas de o acertar ficaram obsoletas pela propria edicao que as escrevia. Um numero que se conta a si proprio pertence ao instrumento, nao a prosa.
  - Dois bloqueadores vieram da revisao independente, ambos com reproducao. O filtro de recusa comparava por prefixo de string e nao por fronteira de caminho, portanto `.project/state.yml.bak`, `.project/last-ci-run.yml.orig`, `.project/checkpointsX.txt` e `.project/checkpoints-archive/secret.yml` entravam num commit de bookkeeping e eram empurrados, enquanto o docstring prometia recusa -- e sem `scan_for_secrets`, que so vivia dentro do `command_commit`. E os cinco testes continuavam verdes com `git add -A` no lugar do pathspec, porque nenhum deles tinha ficheiro nao-bookkeeping FORA do index. Fechados com a regra de fronteira que o `ownership_allows` ja usava, com o scan de segredos, com um teste de trabalho por commitar fora do index, e com mais duas entradas de mutacao.
  - `commit_bookkeeping` dobra as escritas do pipeline num commit proprio, simetrico com o que a T303 ja fazia no `commit`. Fixa os caminhos em `BOOKKEEPING_PATHS` e recusa-se a correr com qualquer outro caminho ja em stage: um comando que faz `git add` em nome do operador nao pode varrer trabalho dele para um commit cuja mensagem nao foi ele que escreveu. Testes medem a ARVORE DEPOIS do comando, nao o conteudo dos ficheiros -- o defeito nunca foi o que se escreve, foi ficar por commitar -- com `origin` a ser um repositorio bare em disco, portanto os dois pushes sao pushes a serio e o segundo e verificado do lado que recebe.
  - O primeiro `publish` real por este caminho encontrou o que nenhum teste tinha: tudo o resto ficou commitado e sobrou `M .project/last-ci-run.yml`. Esse ficheiro e o recibo do CI local, escrito pelo hook de pre-push, e o conteudo dele NOMEIA a cabeca que acabou de verificar -- portanto o segundo push do `publish` regenera-o contra o proprio commit de bookkeeping que esse push transporta. Nenhuma ordenacao resolve: um recibo sobre a HEAD nao cabe dentro da HEAD. Passou a git-ignored, como o `.project/delivery/`, que e o que sempre foi em substancia, e saiu de `BOOKKEEPING_PATHS`. Dois testes seguram os dois sentidos: nenhum caminho que o `commit_bookkeeping` faz stage pode estar ignorado (`git add --` falha em pathspec ignorado, e partiria todos os publish), e o recibo tem de continuar ignorado e fora dos registos. Os meus testes nao o apanharam porque o duplo do `uv` so escrevia um checkpoint e nao havia hook de pre-push no repositorio de teste.
  - Custo aceite e declarado, nao descoberto: o commit de bookkeeping move a cabeca da branch, portanto a pull request actualiza e a CI corre mais uma vez por `publish`. A alternativa era uma excepcao no `start` para caminhos que ele teria de confiar, e uma excepcao num portao nao e conserto da causa. A revisao confirmou que nao ha ordenacao que o evite dentro desta rota: o `gh pr create` precisa da branch no remoto e o registo precisa do numero da pull request, portanto o registo nao pode existir antes do primeiro push.
  - O `merge` NAO fica coberto. A primeira versao desta justificacao afirmava a conclusao a partir de evidencia que nao a sustentava, e a revisao apanhou-a: o ruleset `protect-main` exige mesmo pull request com `bypass_actors` vazio (`gh api repos/:owner/:repo/rulesets/23734100`), mas "o registo nao chega a main" e "a arvore nao pode ficar limpa" sao afirmacoes diferentes, e eu deduzi a segunda da primeira. A rota que me escapou e um fold local sem push; o que a rejeita e ficar o registo orfao numa branch cujo remoto o `merge` apaga, e nao o ruleset. E a razao mais forte de todas nem estava escrita: antecipar a escrita para antes do `gh pr merge` move a cabeca depois de `signed_verdict_refusal` ter medido `headRefOid`, portanto trocaria arvore suja por conteudo que o portao de merge nao mediu. Fica parcial na etapa 9 com estas quatro razoes.
  - Efeito lateral ja documentado no proprio codigo: o comentario em `command_merge` regista que `gh pr merge --delete-branch` falhou com `Your local changes would be overwritten by checkout` em duas pull requests reais, por causa de uma arvore que o `publish` sujara -- nao por algo que o `merge` fizesse.
