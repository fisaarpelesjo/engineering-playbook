---
description: Engineering playbook governance
alwaysApply: true
---

Use `AGENTS.md` and `ENGINEERING.md` as canonical instructions. Read the PRD when present before Spec Kit work. Resume with `uv run python scripts/resume.py`, validate with `uv run python scripts/verify.py`, and checkpoint before stopping.
