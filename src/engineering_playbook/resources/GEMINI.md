# Gemini CLI Adapter

Carregue `AGENTS.md`, `ENGINEERING.md`, `.project/project.yml` e `.project/state.yml`. Leia o PRD quando existir antes de acionar Spec Kit. Retome pelo comando `uv run python scripts/resume.py`. Use specification, plan e tasks ativos como escopo. Evidencias devem corresponder a comandos realmente executados.
